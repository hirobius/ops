# Violating exemption example

This file contains an unknown exemption marker that the gate should reject.

Some component code uses `// widget-ok: custom reason` which is not in the allowed list.

Another case: `// layout-ok:` with no reason at all.
