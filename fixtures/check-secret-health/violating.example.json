{
  "_comment": "REAL violating fixture for check-secret-health (ops#415). Two real failure shapes at once: a workflow references CHROMATIC_PROJECT_TOKEN (real, from hds's chromatic.yml) which the registry snapshot here forgot to carry — the sync drift this check exists to catch — and RALPH_WATCHDOG_TOKEN comes back with a rejected (401) probe, the exact 'silently lapsed' failure RELEASE_PAT motivated (hds#267). Not a stub — both violations fire.",
  "registryNames": ["RALPH_WATCHDOG_TOKEN"],
  "grepNames": ["RALPH_WATCHDOG_TOKEN", "CHROMATIC_PROJECT_TOKEN"],
  "verdictInputs": [
    {
      "entry": {
        "name": "RALPH_WATCHDOG_TOKEN",
        "repos": ["hirobius/ops"],
        "probe": "github-token-expiry",
        "probeStrength": "verified",
        "rotateUrl": "https://github.com/settings/personal-access-tokens",
        "breaksWhenMissing": "the wedge-detection job stops silently"
      },
      "outcome": { "status": "invalid", "detail": "HTTP 401 from GET /user" }
    }
  ]
}
