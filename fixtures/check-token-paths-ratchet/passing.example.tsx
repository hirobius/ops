// passing fixture for check-token-paths-ratchet
// No token-path references — clean file with no design system token strings.

export function PassingExample() {
  return <div className="bg-white text-black">passing example, no token refs</div>;
}
