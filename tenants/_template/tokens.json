{
  "$schema": "../../hirobius.tenant-tokens.schema.json",
  "$description": "Per-tenant token overlay starter. Copy this file to tenants/<slug>/tokens.json and add your brand overrides. See README.md and docs/architecture/tenant-token-overlay-format.md for the rules. Every key under a token group becomes a token path; this template is intentionally empty so the validator does not yield example leaves. Uncomment and adapt the block below to override semantic.accent.rest, the most common starting point.",

  "_uncomment_to_activate_a_brand_accent_override": "Replace the leading underscore on the 'semantic' key (and the placeholder hex values) below to activate. Then run `pnpm tokens` to compile.",

  "_semantic": {
    "accent": {
      "$type": "color",
      "rest": {
        "$value": "#888888",
        "$description": "Tenant primary accent — PLACEHOLDER, replace with brand hex.",
        "$extensions": {
          "com.figma.variables": {
            "modes": {
              "Light": "#888888",
              "Dark": "#666666"
            }
          }
        }
      },
      "hover": {
        "$value": "#777777",
        "$description": "Tenant accent hover state — PLACEHOLDER.",
        "$extensions": {
          "com.figma.variables": {
            "modes": {
              "Light": "#777777",
              "Dark": "#555555"
            }
          }
        }
      },
      "pressed": {
        "$value": "#666666",
        "$description": "Tenant accent pressed state — PLACEHOLDER.",
        "$extensions": {
          "com.figma.variables": {
            "modes": {
              "Light": "#666666",
              "Dark": "#444444"
            }
          }
        }
      }
    }
  }
}
