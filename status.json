{
  "updatedAt": "2026-09-16T08:05:00Z",
  "phase": "live",
  "headline": "ONE MIGRATION NEVER REACHED THE DATABASE — NOT TWO (2026-09-16): the earlier headline said digest_items (0011) AND pitch_queue (0012) were both missing. pitch_queue is a FILENAME, NOT A TABLE — nothing in the repo creates one. 0012 creates lead_notes plus leads.assigned_to, leads.next_action_at, three indexes and RLS, and all six are verified live, so /ops/pitch is NOT reading a store that isn't there. Only 0011 (digest_items) genuinely needs applying; ops#78 is closed with its table absent. ops#348 is corrected on the issue — do not run its SQL as originally written. Also this week: CI on main passed for the first time in months once #342 removed the 8 permanently-red specs (ops#307), and #322 fixed the one chain stage whose proof could be a dead link — published counted a URL STORED in a column, which a 404 satisfies as well as a live site; stage 5 now probes server-side and a URL it could not reach reads unchecked, NEVER dead. Email crawler MERGED (#346) but never run against a real trades site — both containers 403 all egress. Branch graveyard pruned 170 -> 76; the session board is a hard rule in CLAUDE.md §0. THE FUNNEL STILL READS 0 CONTACTED.",
  "next": [
    "MAKE CALLS — 0 contacted is the computed break and has been zero since the table existed. node scripts/export-call-list.mjs --limit 100 > calls.csv (261 eligible, 100 queued, 38 with a real opening line); log every dial including no-answers via scripts/log-call.mjs. One call is NOT gated by the compliance chain.",
    "ops#348 — apply 0011_digest_items ONLY. Its pitch_queue half is wrong (a filename, not a table; everything 0012 creates is already live, and the migration is idempotent so a re-run is harmless but pointless). Ledger repairs for 0010/0012/0013 stand, and scripts/check-migration-ledger.mjs is the highest-value box — repo<->database is the only schema seam here with no gate, which is how a filename got read as a table. NEEDS ADRIAN: writes production.",
    "Re-open ops#78 or file its remainder — it closed with digest_items absent, so its acceptance criteria are not met on production.",
    "ADRIAN: set PAGESPEED_API_KEY in Vercel — free, ~2 min, unblocks audit-sites.mjs so 223 leads get a real opening line.",
    "PR #346 (email crawler) — MERGED, never run for real. Extraction proven on live content; hit rate across the 223 lead sites unknown because both containers block egress. Run it where egress works (a local machine, or a Vercel function); expect tuning, not a clean run.",
    "ops#330 — gate telemetry structurally unfillable: a failing pre-commit gate aborts the commit so post-commit never logs it. CARRIES ralph-wip — the loop holds it, do not start it. (ops#329 landed in #349.)",
    "Eyeball site variety across a batch — site-engine#190 is merged and wired, but nobody has looked at N generated sites side by side to confirm they read as different.",
    "ops#306 — curate gate severity on PROVABLE firing (fixture proof), not firing history. 32 of 53 fixtures are still stubs.",
    "client-repo#51 — 10 claude/* branches audited against the client repo's new main. The earlier note here (\"9 orphan branches, disjoint histories, content a subset of main\") was WRONG: 8 are safe to delete (7 contained in main, 1 superseded by #37+#38 — merging it would REVERT work), and 2 hold genuinely unmerged work needing a human: customer-facing lead-ack copy awaiting owner review (client-repo#3), and a wholesale index.html rewrite 1112 -> 541 lines that runs against the client repo's own 'edit surgically, do not rewrite wholesale' invariant on a live client page (client-repo#46). Separately, the 70 leftover ops branches: 8 moved after merge, 9 closed unmerged, 53 never had a PR — per-branch look, not a sweep.",
    "ADRIAN: remove PORTAL_HMAC_SECRET + VITE_PORTAL_HMAC_SECRET from Vercel — nothing reads either var since the in-ops portal moved to portal-kit.",
    "ADRIAN: ops#200 (Stripe) — there is no way to take money today.",
    "ADRIAN: decide hds 0.14.0 (hds#199) — release path armed but zero changesets, NPM_TOKEN unverified. No agent writes the changeset.",
    "One shared hirobius/ralph engine release covering #303/#302/#296/#238 — not four separate ones.",
    "Compliance, to unlock the SCALED send: #35 -> #38 -> #27, then #9.",
    "Cutover Part B remainder — gated on the clients Astro factory being live."
  ],
  "blocked": []
}
