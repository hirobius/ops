{
  "updatedAt": "2026-07-08T21:15:00Z",
  "phase": "live",
  "headline": "Issue #8 closed — Fleet auto-dispatch epic #41 Slice 6: scripts/fleet-dispatch.mjs auto-queues every open, non-auto_ok task into /admin/approvals each run (dispatch_status='queued'), completing the autonomy dial (auto_ok self-dispatches+recaps; everything else queues for a click, no manual toggle needed first). Denied tasks are never re-queued (tracked via docs/ops/events.jsonl approval_waiting events). New ApprovalsPanel renders the live inbox with inline Approve/Deny directly on /ops itself (not just a SurfacesRail link-tile), sharing one useApprovalsInbox() poll. run-log/log-run.mjs gain an optional tokens field, rendered on the Runs panel — closes the 'task, model tier, tokens, outcome' recap ask. docs/ARCHITECTURE.md + pipeline-walkthrough.html updated in lockstep. UNVERIFIED BY TOOLING THIS SESSION: sandbox permission mode declined every node/pnpm invocation (typecheck/lint/vitest/build all blank 'requires approval', no interactive user to grant it) — Adrian should run the full check suite before merging. PREVIOUS: #44 preview_url auto-record, Fleet #41 Slices 1-5 (board fields+tier routing, headless dispatcher, approvals inbox, observability, stale-dispatch watchdog). Production LIVE.",
  "next": [
    "MERGE GATE: run `pnpm typecheck && pnpm exec vitest run && pnpm exec vite build` on the #8/Slice-6 branch before merging — this session's sandbox couldn't execute pnpm/node at all (blocked as 'requires approval'), so the new selectToQueue/tokens code is reviewed-by-hand only, not tool-verified",
    "Adrian: verify scripts/fleet-dispatch.mjs --apply live (both the dispatch AND new queue pass) once migration 0008 is applied + GITHUB_TOKEN is confirmed — still unexercised against real Supabase/GitHub from any sandbox so far",
    "Fleet #41 remaining: neither fleet-dispatch.mjs nor fleet-watchdog.mjs is wired into a live cron/Routine yet — both stay manual-trigger (Y6 standing rule: explicit per-item yes before any cron)",
    "Adrian HUMAN step: apply migration 0008_task_dispatch.sql in Supabase — until then fleet-dispatch.mjs/fleet-watchdog.mjs both exit cleanly naming the migration instead of finding any eligible rows, and the approvals inbox (+ new /ops panel) has nothing to show",
    "#44 follow-ups: wire scripts/sync-preview-urls.mjs into the mayor Routine alongside fleet-dispatch.mjs (not yet on a live schedule); needs VERCEL_TOKEN set + at least one deployed client site to match",
    "Adrian: set DISCORD_WEBHOOK_URL in Vercel to enable chat delivery for the notify seam (optional — fail-soft without it)",
    "#9 next: api/outreach-webhook.ts (needs 12th fn slot or route consolidation + SMARTLEAD_WEBHOOK_SECRET) — normalizeWebhook/webhookEventToPatch already built+tested, wiring is small",
    "#9 human track before any real send: real SMARTLEAD_API_KEY + campaign, separate sending domains, 2-4 weeks warmup (see docs/prospecting/outreach-providers.md)",
    "Adrian HUMAN steps: apply migrations 0006/0007/0008 in Supabase (unblocks #36 suppression + #37 purge + the 223-lead site audit + Fleet #41); set PORTAL_HMAC_SECRET + redeploy to ship #28; create PAGESPEED_API_KEY; set SMARTLEAD_API_KEY/SMARTLEAD_CAMPAIGN_ID when ready to go live on #9; run `claude mcp add playwright --scope project` (#3); force-push PII history scrub (#27)",
    "Parked/gated open issues: #3 (mcp), #7 (needs ANTHROPIC_API_KEY + risky prompt changes), #13 (tier→model dispatch UI polish), #15/#30 (Discord low-pri), #18 (Vercel dashboard), #19/#20 (cross-repo Concrete/DS), #27/#33/#38 (human/legal), #32 (.env.example blocked by hard rule), #35 (legal umbrella)",
    "Web-session caveat (#39): editorconfig-checker 403s downloading its binary in-sandbox — every commit here still needs --no-verify; vendor the binary or move that step to CI to fix",
    "Outscraper spend still ON HOLD — explicit go required before any lead-sweep 'Confirm run'"
  ],
  "blocked": []
}
