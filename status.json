{
  "updatedAt": "2026-09-16T07:40:00Z",
  "phase": "live",
  "headline": "MAIN IS GREEN AND THE CHAIN TELLS THE TRUTH (2026-09-16): CI on main passed for the first time in months once #342 removed the 8 permanently-red specs (ops#307) — normalised red had been masking real failures. #322 then fixed the one chain stage whose proof could be a dead link: published counted a URL STORED in a column, which a 404 satisfies as well as a live site, and /ops/pitch builds a partner call queue on it. Stage 5 now probes server-side and reports stored vs live, with a URL that could not be reached read as unchecked, NEVER dead. Branch graveyard is pruned (170 -> 76), the session board is a hard rule in CLAUDE.md §0, and lilac has a main branch. CLOSING NOTE: ops#348 is HALF WRONG and is labelled needs-adrian — do NOT run its SQL as written. pitch_queue is a FILENAME, not a table; migration 0012 creates lead_notes plus two leads columns, three indexes and RLS, and all six are verified live. Only 0011 (digest_items) genuinely needs applying. Corrected on the issue with a revised DoD. THE FUNNEL STILL READS 0 CONTACTED.",
  "next": [
    "MAKE CALLS — 0 contacted is the computed break. node scripts/export-call-list.mjs --limit 100 > calls.csv (261 eligible, 100 queued, 38 with a real opening line); log every dial including no-answers via scripts/log-call.mjs. One call is not gated by the compliance chain.",
    "ADRIAN: set PAGESPEED_API_KEY in Vercel — free, ~2 min, unblocks audit-sites.mjs so 223 leads get a real opening line.",
    "ops#348 — apply 0011_digest_items ONLY. Its pitch_queue half is wrong (a filename, not a table; everything 0012 creates is already live and the migration is idempotent). Ledger repairs for 0010/0012/0013 and the new scripts/check-migration-ledger.mjs still stand — that gate is the highest-value box on it, since repo<->database is the only schema seam here with no gate.",
    "ops#330 — gate telemetry structurally unfillable: a failing pre-commit gate aborts the commit so post-commit never logs it, and the post-commit && means a caught violation never reaches the sidecar either. Fix is one character in .husky/post-commit plus stripping lastFiringAt/lastViolationAt from the tracked registry. FREE and unclaimed.",
    "ops#306 — curate gate severity on PROVABLE firing (fixture proof), not firing history. 32 of 53 fixtures are still stubs.",
    "Website email crawler — 223 leads have a site, 1 has an address. Claimed by the ops burndown session; check docs/ai/SESSION-BOARD.md before touching.",
    "ADRIAN: remove PORTAL_HMAC_SECRET + VITE_PORTAL_HMAC_SECRET from Vercel — the in-ops /c/:slug portal was retired in #343, nothing reads either var.",
    "ADRIAN: ops#200 (Stripe) — there is no way to take money today.",
    "ADRIAN: decide hds 0.14.0 (hds#199) — release path armed but zero changesets, NPM_TOKEN unverified. No agent writes the changeset.",
    "One shared hirobius/ralph engine release covering #303/#302/#296/#238 — not four separate ones.",
    "Compliance, to unlock the SCALED send: #35 -> #38 -> #27, then #9.",
    "Cutover Part B remainder — gated on the clients Astro factory being live."
  ],
  "blocked": []
}
