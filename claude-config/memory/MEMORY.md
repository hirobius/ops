# Memory Index

Use memory sparingly.

Default:

- read `project_adrianmilsap.md` only if project history or durable repo context matters
- read `user_adrian.md` only if operator preferences matter

Archive-only:

- `feedback_build_order.md`
- `project_accent_architecture.md`

If a memory file is stale, trim or replace it instead of adding another one.
