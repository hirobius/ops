# @hirobius scope resolves from GitHub Packages (design-system is published there).
# Auth token is injected from the environment — NEVER hardcode it here.
# Set NODE_AUTH_TOKEN to a GitHub token with read:packages scope before install.
@hirobius:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
