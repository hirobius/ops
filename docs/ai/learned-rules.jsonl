{"rule": "A Ralph park citing `iteration ended without a pushed branch` may be loop infrastructure rather than the issue's work — check the claude-step outcome and the agent's own comment before believing the issue is stuck.", "rationale": "ops#44's two attempt-failures were claude-code-action rejecting bot-actor pushes, fixed in #119/#120; the issue was fine. Signal appeared independently on #39 and #240 too.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#44", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "Before re-queuing a previously blocked or parked issue, read its comment history — `closed_by_pull_requests` plus the current code is not sufficient evidence the blocker is gone.", "rationale": "ops#142 was re-queued by a 2026-09-14 burndown sweep that checked only closed PRs and code; it burned a full iteration rediscovering a blocker recorded in July. Adrian's own follow-up generalised the rule.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#142", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "When a gate fails citing a missing binary or dependency, install it and re-run before trusting that diagnosis — the real fault can be a stale config or an architectural mismatch underneath.", "rationale": "ops#178 parked blaming a missing Playwright Chromium binary; with Chromium installed the real faults were a stale DEFAULT_ROUTES list and a gate architecturally unable to test /ops/* (vite preview doesn't run the serverless gate API).", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#178", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "A `ralph-gate` startup_failure with zero jobs run means caller/reusable permission or version skew on a stale branch — update the branch from `main` before concluding the shared engine regressed.", "rationale": "ops#144 root-caused the #133 outage: the engine's gate job began requesting actions:write, which a reusable can never exceed its caller's grant for; only pre-ops#132 branches lacked it.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#133", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "Before executing a deletion or cleanup issue, diff its stated premise against current `main` — a prior unrelated PR may have solved the problem differently, making the literal DoD stale. Stop and ask rather than deleting working code on the issue text's word.", "rationale": "ops#122's target dead code no longer existed as described; PR #124 had rescoped the suites a different way. The deletion was still wanted, but only a human could confirm that.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#122", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "Before tagging an issue ralph-ready, confirm the file or script it targets actually exists in this repo — stale board imports point at other repos.", "rationale": "ops#63 targeted scripts/check-typography-discipline.mjs, which has no history in ops; the stack trace pointed at the separate portfolio repo, already flagged do-not-re-file.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#63", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "Backlog items describing pre-extraction design-system structure (HDS pages, /hds routes) are suspect — ops consumes @hirobius/design-system and no longer authors or hosts it.", "rationale": "ops#70 wanted HDS doc pages absorbed into /ops/atlas; those pages had been moved out to the standalone design-system repo entirely.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#70", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "Before treating a `ralph-blocked: fix is in an open unmerged PR` as resolved, verify that PR's mergeable_state — a stale or conflicting branch stalls the whole queue under single-flight and must be closed and re-dispatched, not waited on.", "rationale": "ops#233's PR #235 went stale and conflicted with main after #244; a human closed it and re-dispatched, landing as #246.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#233", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "An issue whose DoD requires editing `.github/workflows/*` must be split before queueing: the workflow file goes to a human or an adr-eng PR, the rest is re-filed as a script/registry-only issue Ralph can push.", "rationale": "The bot's token lacks the workflows scope. ops#90, #240, #241 and #243 each independently did the full work and then died at the push — four issues rediscovering the same documented wall.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#90,#240,#241,#243", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "A pre-commit gate must not hard-fail the hook on a warn-severity finding, and a pre-commit failure should be checked for reproducibility on `main` before being treated as this issue's fault.", "rationale": "ops#262: check-validator-wiring's substring matcher read a prose mention of 'audit-sbom' in a quality.yml comment as live wiring, emitting a warn-severity WIRING_DRIFT. run-gates.mjs/husky fail on any non-zero exit regardless of severity, so a false positive blocked every commit repo-wide.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#262", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "The harness park comment must echo the agent's own `ralph-blocked` reason rather than substituting a generic diagnosis — a mismatched park reason hides the true state from whoever reviews it.", "rationale": "ops#103's park said 'branch pushed but PR creation failed after 3 attempts (auth/network?)'. The agent had just correctly reported the issue was already shipped in merged PR #192 and no push had failed.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#103", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "A PR body's `Closes #N` is not proof the issue will auto-close — verify the transition after merge and close directly if it didn't, or the loop re-claims finished work indefinitely.", "rationale": "ops#156 accumulated 12+ ralph-blocked re-claims over ~12 hours on already-shipped work; ops#103, #3, #205, #210 and #282 show the same pattern.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#156,#103,#205,#210", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule": "Treat a `ralph-blocked` comment that does not result in the issue actually being parked within one cycle as a harness defect to fix, not a reason to re-dispatch.", "rationale": "ops#156: 5+ blocked comments accumulated before parking stuck. The agent named the likely cause itself — reconcile_issue's blocked-parking path.", "applies_to": "all", "source": "park-harvest", "evidence_unit_id": "ops#156", "ts": "2026-09-14T20:26:52.619826Z"}
{"rule":"GitHub's merge/compare APIs require a full 40-character SHA — a short SHA is rejected outright. Read it with `git rev-parse HEAD`, never `--short`, and never retype an abbreviated SHA copied from earlier push output. More generally: the SAME error twice in one session is a signal to change the procedure, not to retry it.","rationale":"Rejected three times on 2026-09-16 while merging #332, #333 and #336 — identical error each time, because the short SHA was being lifted from 'git push' output. Harmless in itself (the API refuses rather than acting on the wrong ref) but it costs a round trip on every merge, and the repeat is the real finding: an agent that re-makes a mistake inside one session is not reading its own transcript.","applies_to":"all","source":"manual","evidence_unit_id":"ops#336","ts":"2026-09-16T01:27:34.076Z"}
{"rule": "Before citing a stored field as evidence that something happened, find the code that WRITES it and the trigger that runs that code. A field that nothing can fill is not evidence of absence \u2014 it is a broken gauge.", "rationale": "`lastViolationAt` in docs/guardrails/registry.json reads as 'this gate has never caught anything', but it is structurally unfillable for two independent reasons: a pre-commit gate failure aborts the commit, so post-commit never runs; and the `&&` in .husky/post-commit:16 gates the bake on gate SUCCESS, so the one path that could write it only runs when there is nothing to record. Filed as ops#330.", "applies_to": "all", "source": "manual", "evidence_unit_id": "ops#330", "ts": "2026-09-16T06:05:00.000Z"}
{"rule": "Unit tests over fixture HTML do not prove an extractor works. Run the real fetch path against ANY reachable live host before claiming it does \u2014 in a network-restricted container, check the proxy's noProxy list for one.", "rationale": "lib/leads/email-extract.mjs passed 28 fixture tests, then returned `npm-oidc-no-reply@github.com` on the first live fetch (registry.npmjs.org, reachable because it is in the proxy allowlist). JUNK_LOCALPARTS was an exact-match Set containing 'no-reply'; the localpart merely CONTAINED it, so every bounce-noreply@ and mailer-daemon@ had a free pass. One real sample found what 28 hand-written ones could not, because fixtures encode what the author already thought of.", "applies_to": "all", "source": "manual", "evidence_unit_id": "ops#346", "ts": "2026-09-16T06:05:01.000Z"}
{"rule": "A closed issue is not proof its schema change reached the database. For any issue whose DoD includes a migration, verify the table/column exists in the live project before closing.", "rationale": "ops#78 is closed, but `digest_items` does not exist in vvyccwxtcwvlusweenje \u2014 and neither does `pitch_queue` (migration 0012). Repo migrations and the live ledger had diverged three separate ways with nothing gating that seam, while site-engine<->ops and site-engine<->snapshot both have gates. Filed as ops#348.", "applies_to": "all", "source": "manual", "evidence_unit_id": "ops#348", "ts": "2026-09-16T06:05:02.000Z"}
{"rule":"A migration's FILENAME is a label for the change, not an inventory of the objects it creates. Read the SQL before asserting a migration did or did not run — and never infer 'unapplied' from the absence of a table named after the file.","rationale":"ops#348 (p1, sev2, needs-adrian) concluded that 0012_pitch_queue.sql was never applied because no table named pitch_queue exists. No table by that name is created by any migration in the repo: 0012 creates lead_notes plus two leads columns, three indexes and RLS — all six objects verified present in vvyccwxtcwvlusweenje on 2026-09-16. The disproof was already inside the issue: its own query returned lead_notes and it was read as unrelated. The issue would have had a human run DDL on production on a half-wrong premise. Same family as 'find a stored field's writer before citing it as evidence' — here the artefact was a filename.","applies_to":"all","source":"manual","evidence_unit_id":"ops#348","ts":"2026-09-16T07:34:00.000Z"}
{"rule":"Do not write a claim into always-on context (HANDOFF.md, status.json, CLAUDE.md) from memory. Re-verify it at the moment you write it. Always-on context is read and ACTED ON by every later session, so a wrong line there costs an iteration per reader, not per author.","rationale":"On 2026-09-16 this session wrote '9 client-repo orphan branches, disjoint histories, content a subset of main' into ops status.json from recall of an earlier branch listing. Audited: there are 10, only 2 are unmerged, and one is a NET DELETION (index.html 1112 -> 541 lines on a live client page), not a subset. It happened one hour after this same session corrected another session for shipping a wrong migration warning into HANDOFF's Now section — so the failure is not rare, it is the default when a claim is cheap to state and nobody re-reads it before it lands. Corollary seen in the same audit: a branch's commit message is not evidence its work is unmerged; claude/autonomous-issue-handling-el4olq read as a 236-line unmerged feature but main already had it and had moved past it, so merging would have reverted work.","applies_to":"all","source":"manual","evidence_unit_id":"client-repo#51","ts":"2026-09-16T08:05:00.000Z"}
