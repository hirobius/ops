# Normalize line endings to LF for all text files
* text=auto eol=lf

# Explicitly binary — never touch these
*.woff2 binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.ico   binary
